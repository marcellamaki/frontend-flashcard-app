import React from 'react'

const DeckDropdown = (props) => {

		return <option value={props.deck.id}>{props.deck.name}</option>
		//hopefully this is working now!
}

export default DeckDropdown
//
// <div>
//
// </div>
